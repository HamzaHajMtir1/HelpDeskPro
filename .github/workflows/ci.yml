# =============================================================================
# CI — build & test  (PLACEHOLDER — not implemented yet)
# =============================================================================
# Manual trigger only for now, so this placeholder never runs on push/PR.
# Enable the `push` / `pull_request` triggers below when the real jobs land.
#
# Planned scope
# -------------
# frontend (React 19 + Vite 8, npm, Node ^20.19 || >=22.12):
#   - actions/setup-node with cache: npm, cache-dependency-path: frontend/package-lock.json
#   - npm ci
#   - npm run lint            (eslint 9, flat config in frontend/eslint.config.js)
#   - npm test                (NOTE: no test script / test runner exists yet —
#                              add Vitest before wiring this step)
#   - npm run build           (vite build -> frontend/dist)
#
# backend (Spring Boot 3.3.5, Java 17, Maven wrapper 3.9.12):
#   - actions/setup-java, distribution: temurin, java-version: 17, cache: maven
#   - ./mvnw -B -ntp verify
#   - HelpdeskApplicationTests loads the full context -> needs a database:
#     run a postgres:16 service container and override SPRING_DATASOURCE_*,
#     JWT_SECRET, SMTP_USER, SMTP_PASSWORD, GROQ_API_KEY from GitHub Secrets.
#   - upload target/helpdesk-*.jar as an artifact
#
# docker build validation:
#   - docker/setup-buildx-action
#   - docker build ./frontend and ./backend (build only, no push, load: false)
#   - docker compose config  (validate the root compose file)
# =============================================================================

name: CI

on:
  workflow_dispatch:
  # push:
  #   branches: [main]
  # pull_request:
  #   branches: [main]

permissions:
  contents: read

jobs:
  placeholder:
    name: CI placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Not implemented yet
        run: echo "CI pipeline is not implemented yet - see the comments in this file."
