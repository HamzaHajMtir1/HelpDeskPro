# =============================================================================
# Deploy — build, publish to GHCR, release to the OVH VPS
# (PLACEHOLDER — not implemented yet)
# =============================================================================
# Manual trigger only, and nothing is pushed or deployed from this file yet.
#
# Planned scope
# -------------
# build & push images:
#   - docker/setup-buildx-action
#   - docker/login-action against ghcr.io using the built-in GITHUB_TOKEN
#     (no extra secret required) with `packages: write` permission
#   - docker/build-push-action for ./frontend and ./backend
#   - tags: ghcr.io/<owner>/helpdeskpro-{frontend,backend}:{sha,latest}
#   - docker/metadata-action for tags/labels, plus GHA build cache
#
# deploy to the OVH VPS over SSH:
#   - appleboy/ssh-action (or a plain ssh step with a deploy key)
#   - required GitHub Secrets (names only — never values in this repo):
#       VPS_HOST, VPS_USER, VPS_SSH_PRIVATE_KEY, VPS_PORT
#   - on the VPS:
#       docker login ghcr.io
#       cd /opt/helpdeskpro
#       docker compose pull
#       docker compose up -d --remove-orphans
#       docker image prune -f
#   - the VPS keeps its own /opt/helpdeskpro/.env (DB_PASSWORD, JWT_SECRET,
#     SMTP_USER, SMTP_PASSWORD, GROQ_API_KEY) — it is never committed or
#     transferred by this workflow
#
# guardrails to add:
#   - `environment: production` for required reviewers
#   - `concurrency` group so deploys do not overlap
#   - run only after CI and Security succeed on main
# =============================================================================

name: Deploy

on:
  workflow_dispatch:
  # push:
  #   branches: [main]
  # release:
  #   types: [published]

permissions:
  contents: read
  # packages: write   # needed to push images to GHCR

# concurrency:
#   group: deploy-production
#   cancel-in-progress: false

jobs:
  placeholder:
    name: Deploy placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Not implemented yet
        run: echo "Deployment is not implemented yet - see the comments in this file."
